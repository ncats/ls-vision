import { VChartDirective } from './v-chart.directive';

describe('VChartDirective', () => {
    it('should create an instance', () => {
        const directive = new VChartDirective();
        expect(directive).toBeTruthy();
    });
});
